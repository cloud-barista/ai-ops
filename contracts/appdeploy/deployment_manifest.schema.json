{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://khu.ai/schemas/deployment_manifest.schema.json",
  "title": "KHU Deployment Manifest",
  "description": "Planner-compatible deployment intent referencing a registered App. The optional target_profile_id is only a hint; App Deployer selects a ready Target Profile using the requirement envelope.",
  "type": "object",
  "required": ["schema_version", "kind", "spec"],
  "properties": {
    "schema_version": {"const": "deployment.khu.ai/v1alpha1"},
    "kind": {"const": "DeploymentManifest"},
    "metadata": {
      "type": "object",
      "properties": {
        "name": {"type": "string"}
      }
    },
    "spec": {
      "type": "object",
      "required": ["app_version_id"],
      "properties": {
        "app_version_id": {"type": "string"},
        "target_profile_id": {
          "type": "string",
          "description": "Optional Target Profile hint. When omitted, App Deployer selects a ready matching target."
        },
        "accelerator": {"enum": ["none", "nvidia"]},
        "resources": {
          "type": "object",
          "description": "Planner-selected resource envelope. Target capacity comparison depends on the registered Target Profile inventory.",
          "properties": {
            "cpu": {"type": "string"},
            "memory": {"type": "string"},
            "gpu": {"type": "string"},
            "storage": {"type": "string"}
          }
        },
        "requested_by": {"type": "string"},
        "parameters": {"type": "object", "additionalProperties": true}
      }
    }
  }
}
