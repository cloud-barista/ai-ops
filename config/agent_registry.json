{
  "version": "3",
  "defaults": {
    "ai_application_automation": "AIApplicationAutomationAgent",
    "ai_application_operation_optimization": "OperationOptimizationAgent"
  },
  "agents": [
    {
      "name": "AIApplicationAutomationAgent",
      "korean_name": "AI 응용 자동화 에이전트",
      "role": "Act as the KHU AI application automation decision Agent that joins an ApplicationProfile with a ResourceRecommendation and produces a guarded, platform-neutral deployment request.",
      "responsibilities": [
        "Join application.context.created and resource.recommendation.created using correlation_id and profile_id.",
        "Decide DEPLOY, REJECT, or RETRY from validated application requirements and resource candidates.",
        "Generate a platform-neutral Desired Deployment Spec inside deployment.create.request.",
        "Validate decisions with deterministic Go Guard checks and request corrected upstream input when required.",
        "Summarize deployment.status.changed and optimization.feedback.created without executing infrastructure control."
      ],
      "capabilities": [
        "ai_application_automation",
        "ai_application_deployment_control",
        "deployment_manifest_planning"
      ],
      "bounded_actions": [
        "generate_deployment_decision",
        "generate_desired_deployment_spec",
        "request_application_correction",
        "request_resource_recommendation",
        "summarize_deployment_feedback",
        "generate_deployment_manifest",
        "submit_deployment_manifest",
        "observe_deployment_status",
        "observe_status"
      ],
      "reward_signals": [
        "Positive reward when the manifest is contract-valid and preserves trusted request fields.",
        "Positive reward when AppDeploy reaches RUNNING with bounded polling.",
        "Penalty when the proposal invents a Target, Runtime Adapter, credential, or unsupported field.",
        "Penalty when the proposal cannot pass Go Guard validation."
      ],
      "enabled": true
    },
    {
      "name": "OperationOptimizationAgent",
      "korean_name": "AI ?묒슜 ?댁쁺 理쒖쟻???먯씠?꾪듃",
      "role": "Evaluate deployment status, SLO, resource utilization, and inference metrics and produce a bounded scaling recommendation.",
      "responsibilities": [
        "Join deployment.status.changed and optimization.feedback.created using correlation_id and decision_id.",
        "Decide KEEP, SCALE_OUT, or SCALE_IN without executing infrastructure control.",
        "Return SLO and utilization evidence for deterministic Guard validation."
      ],
      "capabilities": ["ai_application_operation_optimization"],
      "bounded_actions": ["generate_scaling_decision"],
      "reward_signals": [
        "Positive reward for a bounded decision supported by observed SLO and utilization evidence.",
        "Penalty for inventing metrics or exceeding replica limits."
      ],
      "enabled": true
    }
  ]
}
